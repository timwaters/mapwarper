<h2>Export</h2>
<h3>Images</h3>
<ul>
  <li>
    GeoTiff: <%= link_to  "Download rectified GeoTiff", export_map_url(:id => @map, :format => :tif) %>
  </li>
  <li>
    PNG: <%= link_to  "Download rectified PNG", export_map_url(:id => @map, :format => :png) %>
    (and associated <%= link_to ".aux.xml", export_map_url(:id => @map, :format => :aux_xml) %> file)
  </li>
</ul>
<h3>Map Services</h3>
<ul>
  <li>KML: <%= link_to("Download KML file",  map_url(:id => @map, :format => :kml)) %> (for use in Google Earth, etc)</li>
  <li>
    <% josm_url = url_for(wms_map_url(:id => @map)) + "?request=GetMap&version=1.1.1&styles=&format=image/png&srs={proj}&exceptions=application/vnd.ogc.session.inimage&width={width}&height={height}&BBOX={bbox}&" %>
    WMS: <%= link_to("WMS Capabilities URL", url_for(wms_map_url(:id=>@map, :request => "GetCapabilities", :service => "WMS", :version => "1.1.1")) )%>
    (<%= link_to "for JOSM OpenStreetMap Editor", josm_url %>    )
  </li>
  <li>
    <% tile_url = "#{tile_map_base_url(:id => @map.id)}/{z}/{x}/{y}.png" %>
    Tiles (Google/OSM scheme): <%= tile_url %>
  </li>
</ul>
Bibliographic:
<ul>
  <li>
    <%= link_to 'Bibliographic', "http://example.com/alink"  %>
  </li>
</ul>
